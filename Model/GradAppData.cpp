#include "GradAppData.h"

GradAppData::GradAppData(string fName,string lName, string emailad, string stunum, string cor, string area, string programs,string super)
: first(fName),last(lName), email(emailad),stuNum(stunum),course(cor), areas(area), program(programs),supervisor(super)
{
	
}